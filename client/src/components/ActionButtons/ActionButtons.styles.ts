import styled from 'styled-components';

export const ActionButton = styled.button`
  border: none;
  border-radius: 6px;
  padding: 6px 12px;
  background: none;
  cursor: pointer;

  &:first-of-type {
    margin-right: 6px;
  }
`;